# Optional when the API is served from a different origin.
VITE_API_BASE_URL=

# Development-only adapter. Set to false to exercise the real login endpoint.
VITE_USE_DEV_AUTH=true
VITE_DEV_AUTH_TOKEN=dev-caregiver-token
