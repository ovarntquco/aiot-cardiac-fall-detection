/**
 * UC9 / FR13-FR14 placeholder types.
 * TODO: call the AI inference service when a real model and sensor stream exist.
 * @typedef {Object} FallDetectionInput
 * @property {string} patientId
 * @property {number[]} acceleration
 * @property {number[]} gyroscope
 * @property {string} measuredAt
 */

export {};
