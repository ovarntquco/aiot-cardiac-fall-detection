/**
 * UC5 / FR6-FR7 placeholder types.
 * TODO: record SOS events from the wearable and trigger downstream alert flow.
 * @typedef {Object} SosSignalInput
 * @property {string} patientId
 * @property {string} triggeredAt
 * @property {number=} holdDurationMs
 */

export {};
