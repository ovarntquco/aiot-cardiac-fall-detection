/**
 * UC6 / FR8 placeholder types.
 * TODO: expose authorized latest GPS/location records after location storage exists.
 * @typedef {Object} PatientLocation
 * @property {string} patientId
 * @property {number} latitude
 * @property {number} longitude
 * @property {string} recordedAt
 */

export {};
